КОНФИГУРАЦИЯ
Перед запуском тестов необходимо выполнить команды
    docker run --name mysql-container -e MYSQL_ROOT_PASSWORD=yourpassword -d mysql
    docker exec -it mysql-container mysql -u root -p
    CREATE DATABASE testdb;
    CREATE USER 'testuser'@'%' IDENTIFIED BY 'testpass';
    GRANT ALL PRIVILEGES ON testdb.* TO 'testuser'@'%';

РЕЗУЛЬТАТЫ
Покрытие тестами кода связанного с базой данных 85%

29 тестов работает, 25 тестов падает. Ошибки в падающих тестах одинаковые и заключаются в том, что
Column ru.yarsu.db.tables.UsersAbilities.value is not a reference, т.е. используется integer("value"), что создает обычную колонку с целыми числами.
Возможно, для решения проблемы нужно использовать reference()

